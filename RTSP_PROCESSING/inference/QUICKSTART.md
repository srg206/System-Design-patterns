# Быстрый старт

## 1. Запуск сервера

В первом терминале:

```bash
cd /Users/a1111/GO_PROJECTS/patterns/System-Design-patterns/RTSP_PROCESSING/inference
python3 server.py
```

Вы должны увидеть:
```
Инициализация InferenceService...
✓ gRPC сервер запущен на порту 50051
Ожидание запросов...
```

## 2. Запуск клиента

Во втором терминале:

```bash
cd /Users/a1111/GO_PROJECTS/patterns/System-Design-patterns/RTSP_PROCESSING/inference
python3 client.py
```

## Ожидаемый результат

Клиент выведет:

```
============================================================
🤖 gRPC Inference Client
============================================================
📁 Чтение изображения: /path/to/frame_000000.jpg
   Размер файла: XXXXX байт
📤 Отправка изображения на сервер localhost:50051...

✓ Получен ответ от сервера
🔍 Обнаружено объектов: 2

   [1] Объект: person
       Координаты:
         x0 = XXX.XX
         y0 = XXX.XX
         x1 = XXX.XX
         y1 = XXX.XX
       Размер: XXX.XX x XXX.XX

   [2] Объект: car
       Координаты:
         x0 = XXX.XX
         y0 = XXX.XX
         x1 = XXX.XX
         y1 = XXX.XX
       Размер: XXX.XX x XXX.XX

============================================================
✓ Готово!
```

## Возможные проблемы

### Ошибка "Connection refused"

Убедитесь, что сервер запущен в другом терминале.

### Ошибка "File not found"

Убедитесь, что файл `frame_000000.jpg` существует в директории с клиентом.

### Ошибка "ModuleNotFoundError"

Установите зависимости:

```bash
pip install -r requirements.txt
```

И сгенерируйте proto файлы:

```bash
./generate_proto.sh
```

## Подключение к другому серверу

Если сервер запущен на другом хосте или порту:

```bash
python3 client.py your-server:50051
```

